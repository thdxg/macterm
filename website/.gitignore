node_modules/

# Build artifacts — regenerated by `bun run build` (runs automatically before
# `bun run dev` / `bun run deploy`). Source lives in docs/, src/, and index.html.
public/docs/
public/tailwind.css
